query DraftById($id: ObjectId!) {
  draft(id: $id) {
    id
    title
    content {
      markdown
    }
    author {
      id
      name
      username
    }
    updatedAt
    tags {
      id
      name
      slug
    }
  }
}
